version: ~> 1.0
dist: bionic
language: go
os: linux
jobs:
  include:
    - go: "1.13.x"
    - go: "1.12.x"

install:
  - go get -t ./...
script:
  - diff -u <(echo -n) <(gofmt -d -s .)
  - go test -v ./...

notifications:
  email: false
